/**
 * Constellation Preview Route
 * 
 * Full preview of installation aesthetic using radial gradient technique (Technique A winner).
 * Shows ~80 stationary particles with soft votive glow over cosmic background with atmospheric fog.
 * 
 * Navigate to: http://localhost:3001/constellation-preview
 */

import ConstellationPreview from '@/presentations/p5-constellation/constellation-preview'

export default function ConstellationPreviewPage() {
  return <ConstellationPreview />
}
