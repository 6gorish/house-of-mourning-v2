/**
 * Constellation Preview Route - Light Shader Version
 * 
 * Shows constellation with LIGHTER cosmic shader (more visible background detail).
 * Compare with /constellation-preview (dark version).
 * 
 * Navigate to: http://localhost:3001/constellation-preview-light
 */

import ConstellationPreviewLight from '@/presentations/p5-constellation/constellation-preview-light'

export default function ConstellationPreviewLightPage() {
  return <ConstellationPreviewLight />
}
